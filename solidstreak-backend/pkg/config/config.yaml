log_level:                    -4
max_tg_event_handlers:        10
invoice_expires_in_min:       1 # 15

basic_subscription:
  price:
    tg_stars_per_month:       0
    tg_stars_per_year:        0
    tg_stars_lifetime:        0
  active_habits_limit:        5
  show_ads:                   true

premium_subscription:
  price:
    tg_stars_per_month:       100
    tg_stars_per_year:        1000
    tg_stars_lifetime:        2500
  active_habits_limit:        100
  show_ads:                   false

scheduler:
  max_task_handlers:          10
  task_batch_size_per_source: 100
  task_waiting_duration_ms:   5000
  lock_duration_ms:           60000